"""
Local configuration template - Copy this to local_config.py

DO NOT COMMIT local_config.py - it's for your personal data paths only!

Usage:
1. Copy this file: cp local_config.py.example local_config.py
2. Edit local_config.py with your actual paths
3. The main config.py will automatically use your paths
"""
import os

# Option 1: Use default data/ directory (recommended)
# Just place files in the data/ directory and keep these commented out
# DATA_DIR = os.path.join(os.path.dirname(os.path.dirname(__file__)), "data")
# DIABETIC_DATA_CSV = os.path.join(DATA_DIR, "diabetic_data.csv")
# IDS_MAPPING_CSV = os.path.join(DATA_DIR, "IDS_mapping.csv")

# Option 2: Use custom absolute paths
# Uncomment and modify these lines with your actual paths
# DIABETIC_DATA_CSV = "/Users/YourName/Desktop/my-data/diabetic_data.csv"
# IDS_MAPPING_CSV = "/Users/YourName/Desktop/my-data/IDS_mapping.csv"

# Option 3: Use paths relative to your home directory
# DIABETIC_DATA_CSV = os.path.expanduser("~/Documents/diabetes-data/diabetic_data.csv")
# IDS_MAPPING_CSV = os.path.expanduser("~/Documents/diabetes-data/IDS_mapping.csv")
